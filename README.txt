Howdy

The application to run is 'openGLConsuleApplication1.exe' under the Release folder. The files you need can be found under this folder as well, but I wanted to include everything just in case. The code of the program can be found in the folder 'openGLConsuleApplication1'.
The code is under the 'openGLConsuleApplication1' folder. 
You can drag the object file into the application to run it.

Pressing the buttons 1, 2, 3 togglers the shading used with 1 being flat, 2 being gouraud, and 3 being phong.